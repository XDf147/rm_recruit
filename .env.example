# 首次启动时创建队长账号。密码至少 10 位；生产环境请使用更强的随机密码。
CAPTAIN_USERNAME=captain
CAPTAIN_PASSWORD=replace-with-a-long-random-password
CAPTAIN_DISPLAY_NAME=队长

# 宿主机对外端口
APP_PORT=3000

# 直接通过 http://服务器IP:端口 访问时保持 false；配置 HTTPS 反向代理后改为 true。
COOKIE_SECURE=false
